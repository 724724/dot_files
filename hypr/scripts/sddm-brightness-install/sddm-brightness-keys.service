[Unit]
Description=Handle brightness keys at the SDDM greeter
After=systemd-logind.service

[Service]
Type=simple
ExecStart=/usr/local/bin/sddm-brightness-keys
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
